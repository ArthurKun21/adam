[project]
name = "adam-docs"
version = "0.0.0"
description = "Documentation site for Adam, the Kotlin coroutine-based ADB client library"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "zensical>=0.0.60",
]
